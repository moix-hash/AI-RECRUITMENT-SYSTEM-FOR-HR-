# Trained model statement

No custom trained or fine-tuned model is included with TalentOS.

The project uses deterministic skill matching and scoring implemented in Python. Gemini is an optional external inference API accessed through LangChain only when an API key is configured. Therefore, there is no trained-model file, model checkpoint, or weight artifact to submit.
